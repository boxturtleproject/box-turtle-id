# Dev Hub Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** A `scripts/dev-hub.mjs` Node.js server that discovers all git worktrees, lets you start individual Vite dev servers on demand, shows merge status, and supports removing merged worktrees — all from a web UI at `http://localhost:5100`.

**Architecture:** Single Node.js script using only built-in modules (`http`, `child_process`, `net`, `path`, `url`). Serves a self-contained HTML hub page and a small JSON API. Child Vite processes are owned by the hub and killed on exit.

**Tech Stack:** Node.js ESM (`.mjs`), built-in `http`/`child_process`/`net`, no dependencies.

---

### Task 1: Add npm script + verify Node can run ESM

**Files:**
- Modify: `package.json`
- Create: `scripts/dev-hub.mjs`

**Step 1: Add hub script to package.json**

In `package.json`, add to `"scripts"`:
```json
"hub": "node scripts/dev-hub.mjs"
```

**Step 2: Create the scripts directory and minimal hub file**

```js
// scripts/dev-hub.mjs
import http from 'http';
import path from 'path';
import { fileURLToPath } from 'url';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const REPO_ROOT = path.resolve(__dirname, '..');
const HUB_PORT = 5100;

const server = http.createServer((req, res) => {
  res.writeHead(200, { 'Content-Type': 'text/plain' });
  res.end('Dev Hub OK');
});

server.listen(HUB_PORT, () => {
  console.log(`Dev Hub running at http://localhost:${HUB_PORT}`);
});
```

**Step 3: Verify**

```bash
cd "/Users/robertfabricant/Desktop/FAB/9_TURTLES/Box Turtle ID"
npm run hub
```
Expected: `Dev Hub running at http://localhost:5100` — then `curl http://localhost:5100` returns `Dev Hub OK`.

Kill with Ctrl+C.

**Step 4: Commit**

```bash
git add package.json scripts/dev-hub.mjs
git commit -m "feat: scaffold dev-hub script"
```

---

### Task 2: Implement worktree discovery and `/api/status`

**Files:**
- Modify: `scripts/dev-hub.mjs`

**Step 1: Replace the file with the discovery + status implementation**

```js
// scripts/dev-hub.mjs
import http from 'http';
import { execSync } from 'child_process';
import path from 'path';
import { fileURLToPath } from 'url';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const REPO_ROOT = path.resolve(__dirname, '..');
const HUB_PORT = 5100;
const BASE_PORT = 5173; // main always gets 5173

// --- Worktree discovery ---

function parseWorktrees() {
  const raw = execSync('git worktree list --porcelain', { cwd: REPO_ROOT }).toString();
  const worktrees = [];
  for (const block of raw.trim().split('\n\n')) {
    const wt = {};
    for (const line of block.trim().split('\n')) {
      if (line.startsWith('worktree ')) wt.path = line.slice(9);
      if (line.startsWith('HEAD '))     wt.commit = line.slice(5, 12);
      if (line.startsWith('branch '))   wt.branch = line.slice(7).replace('refs/heads/', '');
      if (line === 'bare') wt.bare = true;
    }
    if (!wt.bare) worktrees.push(wt);
  }
  return worktrees;
}

function isMerged(branch) {
  if (!branch || branch === 'main') return false;
  try {
    execSync(`git merge-base --is-ancestor "${branch}" main`, { cwd: REPO_ROOT, stdio: 'ignore' });
    return true;
  } catch {
    return false;
  }
}

// --- In-memory server state ---
// Map<id, { process: ChildProcess|null, state: 'idle'|'starting'|'ready'|'error' }>
const serverState = new Map();

function buildStatus() {
  const worktrees = parseWorktrees();
  return worktrees.map((wt, i) => {
    const id = String(i);
    const entry = serverState.get(id) ?? { process: null, state: 'idle' };
    return {
      id,
      path:        wt.path,
      branch:      wt.branch ?? '(detached)',
      commit:      wt.commit ?? '???????',
      port:        BASE_PORT + i,
      merged:      isMerged(wt.branch),
      serverState: entry.state,
    };
  });
}

// --- HTTP server ---

function sendJson(res, data, status = 200) {
  res.writeHead(status, { 'Content-Type': 'application/json' });
  res.end(JSON.stringify(data));
}

const server = http.createServer((req, res) => {
  if (req.method === 'GET' && req.url === '/api/status') {
    return sendJson(res, buildStatus());
  }
  res.writeHead(404);
  res.end('Not found');
});

server.listen(HUB_PORT, () => {
  console.log(`Dev Hub running at http://localhost:${HUB_PORT}`);
});
```

**Step 2: Verify**

```bash
npm run hub
# In another terminal:
curl http://localhost:5100/api/status | node -e "const d=require('fs').readFileSync('/dev/stdin','utf8'); console.log(JSON.stringify(JSON.parse(d), null, 2))"
```
Expected: JSON array with one object per worktree, each with `id`, `branch`, `commit`, `port`, `merged`, `serverState: "idle"`.

Kill hub. Commit:

**Step 3: Commit**

```bash
git add scripts/dev-hub.mjs
git commit -m "feat: add worktree discovery and /api/status"
```

---

### Task 3: Implement `/api/start/:id` with child process management

**Files:**
- Modify: `scripts/dev-hub.mjs`

**Step 1: Add imports at the top**

Add to existing imports:
```js
import { spawn } from 'child_process';
import net from 'net';
```

**Step 2: Add port-ready helper after the imports**

```js
function waitForPort(port, timeoutMs = 30_000) {
  return new Promise((resolve, reject) => {
    const deadline = Date.now() + timeoutMs;
    function attempt() {
      const socket = new net.Socket();
      socket.setTimeout(300);
      socket.on('connect', () => { socket.destroy(); resolve(); });
      socket.on('error',   () => { socket.destroy(); retry(); });
      socket.on('timeout', () => { socket.destroy(); retry(); });
      socket.connect(port, '127.0.0.1');
    }
    function retry() {
      if (Date.now() > deadline) return reject(new Error(`Port ${port} not ready after ${timeoutMs}ms`));
      setTimeout(attempt, 500);
    }
    attempt();
  });
}
```

**Step 3: Add the start handler inside the `createServer` callback**

Add this branch before the final 404 handler:

```js
  if (req.method === 'POST' && req.url?.startsWith('/api/start/')) {
    const id = req.url.slice('/api/start/'.length);
    const status = buildStatus();
    const wt = status.find(w => w.id === id);
    if (!wt) return sendJson(res, { error: 'not found' }, 404);

    const existing = serverState.get(id);
    if (existing && (existing.state === 'starting' || existing.state === 'ready')) {
      return sendJson(res, { ok: true, state: existing.state });
    }

    serverState.set(id, { process: null, state: 'starting' });
    sendJson(res, { ok: true, state: 'starting' });

    // Spawn vite asynchronously
    const proc = spawn('npm', ['run', 'dev', '--', '--port', String(wt.port)], {
      cwd: wt.path,
      stdio: 'ignore',
      detached: false,
    });
    serverState.set(id, { process: proc, state: 'starting' });

    proc.on('error', () => serverState.set(id, { process: null, state: 'error' }));
    proc.on('exit',  () => serverState.set(id, { process: null, state: 'error' }));

    waitForPort(wt.port)
      .then(() => {
        const entry = serverState.get(id);
        if (entry) serverState.set(id, { ...entry, state: 'ready' });
      })
      .catch(() => {
        serverState.set(id, { process: null, state: 'error' });
      });

    return; // response already sent
  }
```

**Step 4: Verify**

```bash
npm run hub
curl -X POST http://localhost:5100/api/start/0
# wait 5 seconds, then:
curl http://localhost:5100/api/status | grep serverState
```
Expected: first curl returns `{"ok":true,"state":"starting"}`, then status shows `"serverState":"ready"` for id 0.

**Step 5: Commit**

```bash
git add scripts/dev-hub.mjs
git commit -m "feat: implement /api/start with vite child process management"
```

---

### Task 4: Implement `/api/remove/:id`

**Files:**
- Modify: `scripts/dev-hub.mjs`

**Step 1: Add the remove handler inside `createServer`, before the 404**

```js
  if (req.method === 'POST' && req.url?.startsWith('/api/remove/')) {
    const id = req.url.slice('/api/remove/'.length);
    const status = buildStatus();
    const wt = status.find(w => w.id === id);
    if (!wt) return sendJson(res, { error: 'not found' }, 404);
    if (!wt.merged) return sendJson(res, { error: 'branch not merged into main' }, 400);

    // Kill running server if any
    const entry = serverState.get(id);
    if (entry?.process) entry.process.kill();
    serverState.delete(id);

    try {
      execSync(`git worktree remove --force "${wt.path}"`, { cwd: REPO_ROOT });
      sendJson(res, { ok: true });
    } catch (e) {
      sendJson(res, { error: String(e) }, 500);
    }
    return;
  }
```

**Step 2: Verify**

```bash
npm run hub
# Check status — confirm a merged worktree exists:
curl http://localhost:5100/api/status | node -e "process.stdin.setEncoding('utf8'); let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>console.log(JSON.parse(d).map(w=>w.branch+' merged='+w.merged).join('\n')))"
# POST remove for a merged worktree (use its id):
curl -X POST http://localhost:5100/api/remove/2   # adjust id
# Check status again — that worktree should be gone
```

**Step 3: Commit**

```bash
git add scripts/dev-hub.mjs
git commit -m "feat: implement /api/remove for merged worktrees"
```

---

### Task 5: Implement the HTML hub page

**Files:**
- Modify: `scripts/dev-hub.mjs`

**Step 1: Add the HTML constant at the top of the file (after imports)**

```js
const HUB_HTML = `<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Dev Hub</title>
<style>
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
  body {
    font-family: 'DM Mono', ui-monospace, monospace;
    background: #0f0f0f;
    color: #e0e0e0;
    padding: 2rem;
    min-height: 100dvh;
  }
  h1 {
    font-size: 0.7rem;
    letter-spacing: 0.3em;
    text-transform: uppercase;
    color: #666;
    margin-bottom: 2rem;
  }
  .grid { display: flex; flex-direction: column; gap: 1px; }
  .card {
    background: #1a1a1a;
    border: 1px solid #2a2a2a;
    padding: 1rem 1.25rem;
    display: flex;
    align-items: center;
    gap: 1rem;
    flex-wrap: wrap;
  }
  .branch { font-size: 0.875rem; flex: 1; min-width: 200px; }
  .commit { font-size: 0.7rem; color: #555; font-family: monospace; }
  .badges { display: flex; gap: 0.5rem; align-items: center; }
  .badge {
    font-size: 0.6rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    padding: 0.2rem 0.5rem;
    border-radius: 2px;
    white-space: nowrap;
  }
  .badge.merged   { background: #1a2a1a; color: #4a8a4a; border: 1px solid #2a4a2a; }
  .badge.unmerged { background: #2a1a0a; color: #ca7a2a; border: 1px solid #4a2a0a; }
  .badge.idle     { background: #1e1e1e; color: #555;    border: 1px solid #2a2a2a; }
  .badge.starting { background: #1a1a2a; color: #6a6aaa; border: 1px solid #2a2a4a; }
  .badge.ready    { background: #0a1a2a; color: #4a8aaa; border: 1px solid #1a4a6a; }
  .badge.error    { background: #2a0a0a; color: #aa4a4a; border: 1px solid #4a1a1a; }
  .actions { display: flex; gap: 0.5rem; margin-left: auto; }
  button {
    font-family: inherit;
    font-size: 0.65rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    border: 1px solid #333;
    background: #222;
    color: #bbb;
    padding: 0.3rem 0.75rem;
    cursor: pointer;
    border-radius: 2px;
  }
  button:hover { background: #2a2a2a; border-color: #555; color: #eee; }
  button.primary { border-color: #1a4a6a; color: #4a8aaa; }
  button.primary:hover { background: #0a1a2a; }
  button.danger  { border-color: #4a1a1a; color: #aa4a4a; }
  button.danger:hover  { background: #2a0a0a; }
</style>
</head>
<body>
<h1>Box Turtle ID &mdash; Dev Hub</h1>
<div class="grid" id="root"></div>
<script>
  async function poll() {
    try {
      const res = await fetch('/api/status');
      render(await res.json());
    } catch {}
  }

  function render(worktrees) {
    document.getElementById('root').innerHTML = worktrees.map(wt => \`
      <div class="card">
        <div class="branch">\${wt.branch}</div>
        <div class="commit">\${wt.commit}</div>
        <div class="badges">
          <span class="badge \${wt.merged ? 'merged' : 'unmerged'}">\${wt.merged ? 'merged' : 'unmerged'}</span>
          <span class="badge \${wt.serverState}">\${wt.serverState}</span>
          <span class="badge" style="color:#444;border-color:#222">:\${wt.port}</span>
        </div>
        <div class="actions">
          \${wt.serverState === 'idle' || wt.serverState === 'error'
            ? \`<button class="primary" onclick="start('\${wt.id}')">Start</button>\`
            : ''}
          \${wt.serverState === 'ready'
            ? \`<button class="primary" onclick="window.open('http://localhost:\${wt.port}')">Open</button>\`
            : ''}
          \${wt.merged
            ? \`<button class="danger" onclick="remove('\${wt.id}')">Remove</button>\`
            : ''}
        </div>
      </div>
    \`).join('');
  }

  async function start(id) {
    await fetch('/api/start/' + id, { method: 'POST' });
  }

  async function remove(id) {
    if (!confirm('Remove this worktree? This cannot be undone.')) return;
    await fetch('/api/remove/' + id, { method: 'POST' });
  }

  setInterval(poll, 1500);
  poll();
</script>
</body>
</html>`;
```

**Step 2: Update the GET `/` handler in `createServer`**

Replace the old placeholder with:
```js
  if (req.method === 'GET' && req.url === '/') {
    res.writeHead(200, { 'Content-Type': 'text/html' });
    return res.end(HUB_HTML);
  }
```

**Step 3: Verify**

```bash
npm run hub
# Open http://localhost:5100 in a browser
```
Expected: hub page with a card per worktree, badges showing branch/merge/server status, Start and Remove buttons visible as appropriate.

**Step 4: Commit**

```bash
git add scripts/dev-hub.mjs
git commit -m "feat: implement hub HTML page with status badges and action buttons"
```

---

### Task 6: Auto-open browser + SIGINT cleanup

**Files:**
- Modify: `scripts/dev-hub.mjs`

**Step 1: Add browser-open + SIGINT handler after the `server.listen` call**

```js
// Auto-open browser
server.listen(HUB_PORT, () => {
  console.log(`Dev Hub running at http://localhost:${HUB_PORT}`);
  const opener = process.platform === 'win32' ? 'start' : process.platform === 'darwin' ? 'open' : 'xdg-open';
  import('child_process').then(({ exec }) => exec(`${opener} http://localhost:${HUB_PORT}`));
});

// Kill all child Vite processes on exit
process.on('SIGINT', () => {
  console.log('\nShutting down Dev Hub...');
  for (const entry of serverState.values()) {
    if (entry.process) entry.process.kill();
  }
  process.exit(0);
});
```

Note: the `server.listen` call already exists — replace it entirely with this version that includes the auto-open and keep the SIGINT handler after it.

**Step 2: Verify**

```bash
npm run hub
```
Expected: terminal prints `Dev Hub running at http://localhost:5100`, browser opens automatically to that URL, hub page loads and shows worktrees.

Press Ctrl+C. Expected: "Shutting down Dev Hub..." printed, no orphaned Vite processes remain.

**Step 3: Commit**

```bash
git add scripts/dev-hub.mjs
git commit -m "feat: auto-open browser and clean up child processes on SIGINT"
```

---

### Task 7: Final end-to-end test

**Step 1: Kill any running Vite processes**

```bash
pkill -f "vite" 2>/dev/null || true
```

**Step 2: Start the hub**

```bash
cd "/Users/robertfabricant/Desktop/FAB/9_TURTLES/Box Turtle ID"
npm run hub
```
Expected: browser opens, hub page shows `main`, `claude/priceless-chatelet`, `feature/site-band`, `claude/wonderful-dhawan` — all `idle`.

**Step 3: Start a worktree**

Click **Start** on `claude/wonderful-dhawan`. Expected: badge changes to `starting…`, then `ready` within ~10 seconds.

**Step 4: Open the worktree**

Click **Open**. Expected: `http://localhost:5175` (or whichever port) opens the Box Turtle ID app.

**Step 5: Verify merged worktrees show Remove**

Any worktree with `merged` badge should have a **Remove** button. Unmerged worktrees should not.

**Step 6: Commit any final adjustments, then note the hub is ready**

```bash
git add scripts/dev-hub.mjs
git commit -m "chore: dev hub complete"
```
